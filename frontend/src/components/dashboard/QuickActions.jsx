import { FileText, Eye, Play, Plus } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
export default function QuickActions() { const navigate = useNavigate(); const actions = [{ label: 'Start Scan', icon: Play, path: '/dashboard/scans' }, { label: 'Create Project', icon: Plus, path: '/dashboard/projects' }, { label: 'Generate Report', icon: FileText, path: '/dashboard/reports' }, { label: 'View Assets', icon: Eye, path: '/dashboard/assets' }]; return <div className="card p-5"><h2 className="mb-4 text-base font-semibold text-slate-100">Quick Actions</h2><div className="grid grid-cols-2 gap-3">{actions.map(x => <button key={x.label} onClick={() => navigate(x.path)} className="flex items-center gap-2 rounded-lg border border-slate-700/60 bg-slate-800/50 px-3 py-3 text-left text-sm text-slate-300 hover:border-sky-500/50 hover:text-sky-300"><x.icon className="h-4 w-4 text-sky-400" />{x.label}</button>)}</div></div> }
